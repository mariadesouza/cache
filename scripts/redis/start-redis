# This can be used to start redis in docker if not using docker for the web-server
# USed for debugging etc.

export SEGMENT_REDIS_SERVER="0.0.0.0"
echo "## Starting Redis container"
docker run --name redis -p 6379:6379 --rm redis
