docker rm $(docker stop $(docker ps -a -q --filter name=segment-webserver --format="{{.ID}}"))
docker rm $(docker stop $(docker ps -a -q --filter name=redis --format="{{.ID}}"))
docker rmi segment-webserver
